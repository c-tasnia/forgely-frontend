VITE_API_URL=http://localhost:5000/api

# From your Pusher app dashboard (pusher.com → Channels → your app → App Keys).
# Must match the backend's PUSHER_APP_ID/cluster. Leave blank to run without
# real-time chat/notifications — the rest of the app works fine either way.
VITE_PUSHER_KEY=
VITE_PUSHER_CLUSTER=
